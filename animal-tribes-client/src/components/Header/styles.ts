import { Navbar as MaterializedNavbar } from 'react-materialize'
import styled from 'styled-components'

export const StyledNavbar = styled(MaterializedNavbar)`
  background-color: #141414;
  padding: 0 20px;
`

export const Logo = styled.a`
  font-family: 'Hanalei Fill', cursive;
`